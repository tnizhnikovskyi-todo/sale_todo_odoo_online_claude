# План збірки — ТОВ «Приклад»

Куплено: База р.1, Рахунки та оплати р.1, Склад р.3, Міграція довідників р.3, Навчання команди р.1, CRM р.3, Продажі р.3, Інтернет-магазин р.3, Точка продажу р.3, Закупівлі р.3, Проєкти й таймшити р.3, Виїзні роботи р.2, Планування р.3, Сервісні заявки р.3, Підписки р.3, Оренда р.2, Обслуговування обладнання р.3, Витрати співробітників р.3, Персонал і відпустки р.3, Документи й підписи р.3, Дашборди й звіти р.3
Кроків до виконання: 332 · викликів конектора: 456

Виконує Claude через зовнішнє API. Після КОЖНОГО кроку, що змінює базу,
перечитати запис: конектор повідомляє помилку й тоді, коли дія відбулася.

## База р.1 · компанія з реквізитами

1. **знайти** `res.country` домен `[["code", "=", "UA"]]` → назвати `$країна`
2. **перевірити** `res.company` домен `[["parent_id", "=", false]]`
   - очікувати щонайменше: 1
3. **знайти** `res.company` домен `[["id", "=", 1]]` → назвати `$компанія`
4. **записати** `res.company` → ціль `$компанія`
   - значення: `{"name": "ТОВ «Приклад»", "street": "вул. Прикладна, 1", "city": "Київ", "zip": "01001", "country_id": "$країна", "phone": "+380000000000", "email": "office@example.com", "website": "https://example.com", "vat": "UA000000000000", "company_registry": "00000000"}`
5. **перевірити** `res.company` → ціль `$компанія`
   - очікувати: `{"name": "ТОВ «Приклад»", "company_registry": "00000000"}`

## База р.1 · українська мова інтерфейсу, часовий пояс і валюта UAH

6. **знайти** `res.lang` домен `[["code", "=", "uk_UA"]]` → назвати `$мова`
7. **записати** `res.lang` → ціль `$мова`
   - значення: `{"active": true}`
8. **знайти** `res.currency` домен `[["name", "=", "UAH"]]` → назвати `$валюта`
9. **записати** `res.currency` → ціль `$валюта`
   - значення: `{"active": true}`
10. **знайти** `res.company` домен `[["id", "=", 1]]` → назвати `$компанія`
11. **записати** `res.company` → ціль `$компанія`
   - значення: `{"currency_id": "$валюта"}`
12. **знайти** `res.users` домен `[["share", "=", false]]` → назвати `$внутрішні_користувачі`
13. **записати** `res.users` → ціль `$внутрішні_користувачі`
   - значення: `{"lang": "uk_UA", "tz": "Europe/Kiev"}`
14. **перевірити** `res.company` → ціль `$компанія`
   - очікувати: `{"currency_id": "$валюта"}`

## База р.1 · користувачі та групи доступу

15. **знайти_xmlid** → назвати `$група_внутрішній`
   - зовнішній id: `base.group_user`
16. **знайти_xmlid** → назвати `$група_продажі_свої`
   - зовнішній id: `sales_team.group_sale_salesman`
17. **знайти** `res.users` домен `[["login", "in", ["sales1@example.com", "sales2@example.com"]]]` → назвати `$продавці`
18. **записати** `res.users` → ціль `$продавці`
   - значення: `{"group_ids": [[4, "$група_продажі_свої"]]}`
19. **перевірити** `res.users` → ціль `$продавці`
   - очікувати_містить: `{"group_ids": "$група_продажі_свої"}`

## База р.1 · довідник контрагентів

20. **створити** `res.partner.category` (тільки якщо немає `[["name", "=", "Клієнт"]]`) → назвати `$тег_клієнт`
   - значення: `{"name": "Клієнт"}`
21. **створити** `res.partner.category` (тільки якщо немає `[["name", "=", "Постачальник"]]`) → назвати `$тег_постачальник`
   - значення: `{"name": "Постачальник"}`
22. **перевірити** `res.partner.category` домен `[["name", "in", ["Клієнт", "Постачальник"]]]`
   - очікувати кількість: 2

## База р.1 · налаштування пошти й повідомлень

23. **перевірити** `mail.alias.domain` домен `[]`
   - очікувати щонайменше: 1
24. **знайти** `res.users` домен `[["share", "=", false]]` → назвати `$внутрішні_користувачі`
25. **записати** `res.users` → ціль `$внутрішні_користувачі`
   - значення: `{"notification_type": "email"}`
26. **перевірити** `res.users` → ціль `$внутрішні_користувачі`
   - очікувати: `{"notification_type": "email"}`

## База р.1 · нумерація документів: префікс, рік, лічильник

27. **знайти** `ir.sequence` домен `[["code", "=", "sale.order"]]` → назвати `$послідовність_замовлень`
28. **записати** `ir.sequence` → ціль `$послідовність_замовлень`
   - значення: `{"prefix": "ЗАМ/%(range_year)s/", "padding": 5, "use_date_range": true}`
29. **перевірити** `ir.sequence` → ціль `$послідовність_замовлень`
   - очікувати: `{"use_date_range": true, "padding": 5}`

## База р.1 · двофакторна автентифікація для адміністраторів

30. **параметр**
   - параметр `auth_totp.policy` = `employee_required`
31. **перевірити** `ir.config_parameter` домен `[["key", "=", "auth_totp.policy"]]`
   - очікувати: `{"value": "employee_required"}`

## Рахунки та оплати р.1 · журнали продажів, купівель, банку й каси

32. **створити** `account.journal` (тільки якщо немає `[["code", "=", "РАХ"]]`)
   - значення: `{"name": "Рахунки клієнтам", "code": "РАХ", "type": "sale"}`
33. **створити** `account.journal` (тільки якщо немає `[["code", "=", "ПОСТ"]]`)
   - значення: `{"name": "Рахунки постачальників", "code": "ПОСТ", "type": "purchase"}`
34. **створити** `account.journal` (тільки якщо немає `[["code", "=", "БАНК"]]`)
   - значення: `{"name": "Банк (основний рахунок)", "code": "БАНК", "type": "bank"}`
35. **створити** `account.journal` (тільки якщо немає `[["code", "=", "КАСА"]]`)
   - значення: `{"name": "Каса (основна)", "code": "КАСА", "type": "cash"}`
36. **перевірити** `account.journal` домен `[["type", "in", ["sale", "purchase", "bank", "cash"]]]`
   - очікувати щонайменше: 4

## Рахунки та оплати р.1 · нумерація рахунків і платежів

37. **знайти** `account.journal` домен `[["type", "=", "sale"]]` → назвати `$журнали_продажів`
38. **перевірити** `account.journal` домен `[["type", "=", "sale"], ["code", "=", "РАХ"]]`
   - очікувати щонайменше: 1

## Рахунки та оплати р.1 · умови оплати й банківські реквізити в документах

39. **створити** `account.payment.term` (тільки якщо немає `[["name", "=", "Оплата протягом 7 днів"]]`) → назвати `$умова_оплати`
   - значення: `{"name": "Оплата протягом 7 днів", "line_ids": [[0, false, {"value": "percent", "value_amount": 100, "delay_type": "days_after", "nb_days": 7}]]}`
40. **знайти** `res.company` домен `[["id", "=", 1]]` → назвати `$компанія`
41. **знайти** `res.partner` домен `[["id", "=", "$компанія.partner_id"]]` → назвати `$контрагент_компанії`
42. **створити** `res.partner.bank` (тільки якщо немає `[["acc_number", "=", "UA000000000000000000000000000"]]`) → назвати `$рахунок_компанії`
   - значення: `{"acc_number": "UA000000000000000000000000000", "partner_id": "$контрагент_компанії"}`
43. **знайти** `account.journal` домен `[["type", "=", "bank"], ["code", "=", "БАНК"]]` → назвати `$журнал_банку`
44. **записати** `account.journal` → ціль `$журнал_банку`
   - значення: `{"bank_account_id": "$рахунок_компанії"}`
45. **перевірити** `account.journal` → ціль `$журнал_банку`
   - очікувати: `{"bank_account_id": "$рахунок_компанії"}`

## Рахунки та оплати р.1 · довідник номенклатури: категорії, одиниці виміру, внутрішні коди

46. **створити** `product.category` (тільки якщо немає `[["name", "=", "Товари"]]`)
   - значення: `{"name": "Товари"}`
47. **створити** `product.category` (тільки якщо немає `[["name", "=", "Послуги"]]`)
   - значення: `{"name": "Послуги"}`
48. **знайти** `uom.uom` домен `[["name", "=", "kg"]]` → назвати `$базова_одиниця`
49. **створити** `uom.uom` (тільки якщо немає `[["name", "=", "Мішок 25 кг"]]`) → назвати `$власна_одиниця`
   - значення: `{"name": "Мішок 25 кг", "relative_uom_id": "$базова_одиниця", "relative_factor": 25}`
50. **створити** `product.template` (тільки якщо немає `[["default_code", "=", "TOV-1"]]`)
   - значення: `{"name": "Приклад товару", "default_code": "TOV-1", "list_price": 100}`
51. **перевірити** `product.template` домен `[["default_code", "!=", false]]`
   - очікувати щонайменше: 1

## Рахунки та оплати р.1 · перевірка стандартного бланка рахунку

52. **перевірити** `ir.actions.report` домен `[["model", "=", "account.move"]]`
   - очікувати щонайменше: 1

## Рахунки та оплати р.1 · реєстрація оплат і зіставлення з рахунками

53. **перевірити** `account.journal` домен `[["type", "in", ["bank", "cash"]]]`
   - очікувати щонайменше: 2

## Склад р.1 · складський бік номенклатури: одиниці виміру, штрихкоди, ознака складського обліку

54. **перевірити** `product.template` домен `[["is_storable", "=", true]]`
   - очікувати щонайменше: 1
55. **перевірити** `uom.uom` домен `[]`
   - очікувати щонайменше: 1

## Склад р.1 · один склад і локації зберігання

56. **знайти** `stock.location` домен `[["complete_name", "=", "WH/Запаси"]]` → назвати `$локація_запасів`
57. **створити** `stock.location` (тільки якщо немає `[["name", "=", "Зона А — стелажі"], ["location_id", "in", "$локація_запасів"]]`)
   - значення: `{"name": "Зона А — стелажі", "location_id": "$локація_запасів", "usage": "internal"}`
58. **створити** `stock.location` (тільки якщо немає `[["name", "=", "Зона Б — холодильник"], ["location_id", "in", "$локація_запасів"]]`)
   - значення: `{"name": "Зона Б — холодильник", "location_id": "$локація_запасів", "usage": "internal"}`
59. **перевірити** `stock.location` домен `[["location_id", "in", "$локація_запасів"]]`
   - очікувати щонайменше: 2

## Склад р.1 · прихід від постачальника

60. **перевірити** `stock.picking.type` домен `[["code", "=", "incoming"]]`
   - очікувати щонайменше: 1

## Склад р.1 · відгрузка клієнту

61. **перевірити** `stock.picking.type` домен `[["code", "=", "outgoing"]]`
   - очікувати щонайменше: 1

## Склад р.1 · звіт про поточні залишки

62. **перевірити** `ir.model` домен `[["model", "=", "stock.quant"]]`
   - очікувати кількість: 1

## Склад р.3 · окреме робоче місце комірника: свій пункт меню, своє подання операцій і свій фільтр

63. **створити** `ir.ui.view` (тільки якщо немає `[["name", "=", "stock.picking.місце.Операції комірника"]]`) → назвати `$подання_місця`
   - значення: `{"name": "stock.picking.місце.Операції комірника", "model": "stock.picking", "type": "list", "arch": "<list string=\"Операції комірника\"><field name=\"name\"/><field name=\"partner_id\"/><field name=\"scheduled_date\"/><field name=\"state\"/></list>"}`
64. **створити** `ir.actions.act_window` (тільки якщо немає `[["name", "=", "Операції комірника"], ["res_model", "=", "stock.picking"]]`) → назвати `$дія_місця`
   - значення: `{"name": "Операції комірника", "res_model": "stock.picking", "view_mode": "list,form", "view_id": "$подання_місця", "domain": "[('state','in',['assigned','confirmed'])]", "context": "{'group_by': ['picking_type_id']}"}`
65. **знайти_xmlid** → назвати `$меню_батько`
   - зовнішній id: `stock.menu_stock_root`
66. **знайти_xmlid** → назвати `$роль_місця`
   - зовнішній id: `stock.group_stock_user`
67. **створити** `ir.ui.menu` (тільки якщо немає `[["name", "=", "Операції комірника"], ["parent_id", "=", "$меню_батько"]]`)
   - значення: `{"name": "Операції комірника", "parent_id": "$меню_батько", "action": "$дія_місця", "group_ids": [[4, "$роль_місця"]], "sequence": 90}`
68. **створити** `ir.filters` (тільки якщо немає `[["name", "=", "До виконання"], ["model_id", "=", "stock.picking"]]`)
   - значення: `{"name": "До виконання", "model_id": "stock.picking", "domain": "[('state','in',['assigned','confirmed'])]", "context": "{}", "sort": "[]", "action_id": "$дія_місця"}`
69. **перевірити** `ir.ui.menu` домен `[["name", "=", "Операції комірника"]]`
   - очікувати кількість: 1

## Міграція довідників р.2 · службові поля для звірки з джерелом

70. **знайти** `ir.model` домен `[["model", "=", "res.partner"]]` → назвати `$модель_поля`
71. **створити** `ir.model.fields` (тільки якщо немає `[["model", "=", "res.partner"], ["name", "=", "x_dzherelo_migracii"]]`) → назвати `$власне_поле`
   - значення: `{"name": "x_dzherelo_migracii", "field_description": "Джерело міграції (id у старій системі)", "model_id": "$модель_поля", "model": "res.partner", "ttype": "char", "state": "manual"}`
72. **знайти** `ir.ui.view` домен `[["name", "=", "res.partner.form"], ["type", "=", "form"], ["inherit_id", "=", false]]` → назвати `$подання_батько`
73. **створити** `ir.ui.view` (тільки якщо немає `[["name", "=", "res.partner.x_dzherelo_migracii"]]`)
   - значення: `{"name": "res.partner.x_dzherelo_migracii", "model": "res.partner", "inherit_id": "$подання_батько", "arch": "<data><xpath expr=\"//sheet\" position=\"inside\"><group string=\"Міграція\"><field name=\"x_dzherelo_migracii\"/></group></xpath></data>"}`
74. **перевірити** `ir.model.fields` домен `[["model", "=", "res.partner"], ["name", "=", "x_dzherelo_migracii"]]`
   - очікувати кількість: 1

## Міграція довідників р.3 · перенесення відкритих замовлень і боргів

75. **перевірити** `ir.model` домен `[["model", "=", "sale.order"]]`
76. **перевірити** `ir.model` домен `[["model", "=", "account.move"]]`

## CRM р.1 · одна воронка зі стадіями

77. **створити** `crm.stage` (тільки якщо немає `[["name", "=", "Новий"]]`)
   - значення: `{"name": "Новий", "sequence": 1}`
78. **створити** `crm.stage` (тільки якщо немає `[["name", "=", "Квалiфiкований"]]`)
   - значення: `{"name": "Квалiфiкований", "sequence": 2}`
79. **створити** `crm.stage` (тільки якщо немає `[["name", "=", "Пропозиція"]]`)
   - значення: `{"name": "Пропозиція", "sequence": 3}`
80. **створити** `crm.stage` (тільки якщо немає `[["name", "=", "У роботі з документами"]]`)
   - значення: `{"name": "У роботі з документами", "sequence": 4}`
81. **створити** `crm.stage` (тільки якщо немає `[["name", "=", "Виграно"]]`)
   - значення: `{"name": "Виграно", "sequence": 5}`
82. **перевірити** `crm.stage` домен `[]`
   - очікувати щонайменше: 5

## CRM р.1 · картка клієнта з контактами

83. **перевірити** `ir.model` домен `[["model", "=", "res.partner"]]`
   - очікувати кількість: 1

## CRM р.1 · ведення угод і активностей

84. **перевірити** `mail.activity.type` домен `[]`
   - очікувати щонайменше: 3

## CRM р.1 · календар зустрічей і дзвінків

85. **перевірити** `ir.model` домен `[["model", "=", "calendar.event"]]`
   - очікувати кількість: 1

## CRM р.1 · звіт за воронкою

86. **перевірити** `crm.stage` домен `[]`
   - очікувати щонайменше: 1

## CRM р.3 · кнопки-дії в картці угоди

87. **знайти** `ir.model` домен `[["model", "=", "crm.lead"]]` → назвати `$модель_кнопки`
88. **знайти** `mail.activity.type` домен `[["name", "=", "Call"]]` → назвати `$тип_активності`
89. **створити** `ir.actions.server` (тільки якщо немає `[["name", "=", "Передзвонити клієнту через 2 дні"], ["model_id", "in", "$модель_кнопки"]]`)
   - значення: `{"name": "Передзвонити клієнту через 2 дні", "model_id": "$модель_кнопки", "state": "next_activity", "activity_type_id": "$тип_активності", "activity_summary": "Передзвонити клієнту", "activity_date_deadline_range": 2, "activity_date_deadline_range_type": "days", "activity_user_type": "generic", "activity_user_field_name": "user_id", "binding_model_id": "$модель_кнопки", "binding_type": "action", "binding_view_types": "form,list"}`
90. **перевірити** `ir.actions.server` домен `[["name", "=", "Передзвонити клієнту через 2 дні"], ["model_id", "in", "$модель_кнопки"]]`
   - очікувати кількість: 1
91. **знайти** `ir.model` домен `[["model", "=", "crm.lead"]]` → назвати `$модель_кнопки`
92. **знайти** `crm.stage` домен `[["name", "=", "КП надіслано"]]` → назвати `$стадія`
93. **створити** `ir.actions.server` (тільки якщо немає `[["name", "=", "→ КП надіслано"], ["model_id", "in", "$модель_кнопки"]]`)
   - значення: `{"name": "→ КП надіслано", "model_id": "$модель_кнопки", "state": "object_write", "update_path": "stage_id", "evaluation_type": "value", "resource_ref": "crm.stage,$стадія", "binding_model_id": "$модель_кнопки", "binding_type": "action", "binding_view_types": "form,list"}`
94. **перевірити** `ir.actions.server` домен `[["name", "=", "→ КП надіслано"], ["model_id", "in", "$модель_кнопки"]]`
   - очікувати кількість: 1

## CRM р.3 · окреме робоче місце під роль: свій пункт меню, своє подання воронки й свій фільтр

95. **створити** `ir.ui.view` (тільки якщо немає `[["name", "=", "crm.lead.місце.Воронка керівника"]]`) → назвати `$подання_місця`
   - значення: `{"name": "crm.lead.місце.Воронка керівника", "model": "crm.lead", "type": "list", "arch": "<list string=\"Воронка керівника\"><field name=\"name\"/><field name=\"partner_id\"/><field name=\"expected_revenue\"/><field name=\"stage_id\"/><field name=\"user_id\"/></list>"}`
96. **створити** `ir.actions.act_window` (тільки якщо немає `[["name", "=", "Воронка керівника"], ["res_model", "=", "crm.lead"]]`) → назвати `$дія_місця`
   - значення: `{"name": "Воронка керівника", "res_model": "crm.lead", "view_mode": "list,form", "view_id": "$подання_місця", "domain": "[('stage_id.is_won','=',False)]", "context": "{'group_by': ['stage_id']}"}`
97. **знайти_xmlid** → назвати `$меню_батько`
   - зовнішній id: `crm.crm_menu_root`
98. **знайти_xmlid** → назвати `$роль_місця`
   - зовнішній id: `sales_team.group_sale_salesman_all_leads`
99. **створити** `ir.ui.menu` (тільки якщо немає `[["name", "=", "Воронка керівника"], ["parent_id", "=", "$меню_батько"]]`)
   - значення: `{"name": "Воронка керівника", "parent_id": "$меню_батько", "action": "$дія_місця", "group_ids": [[4, "$роль_місця"]], "sequence": 90}`
100. **створити** `ir.filters` (тільки якщо немає `[["name", "=", "Угоди в роботі"], ["model_id", "=", "crm.lead"]]`)
   - значення: `{"name": "Угоди в роботі", "model_id": "crm.lead", "domain": "[('stage_id.is_won','=',False)]", "context": "{}", "sort": "[]", "action_id": "$дія_місця"}`
101. **перевірити** `ir.ui.menu` домен `[["name", "=", "Воронка керівника"]]`
   - очікувати кількість: 1

## Продажі р.1 · шаблон комерційної пропозиції

102. **знайти** `product.product` домен `[["default_code", "=", "TOV-1"]]` → назвати `$товар_кп`
103. **створити** `sale.order.template` (тільки якщо немає `[["name", "=", "КП: базовий контур"]]`) → назвати `$шаблон_кп`
   - значення: `{"name": "КП: базовий контур", "number_of_days": 14, "note": "Ціни без ПДВ. ПДВ додається за законодавством.", "sale_order_template_line_ids": [[0, false, {"product_id": "$товар_кп", "product_uom_qty": 1}]]}`
104. **перевірити** `sale.order.template` домен `[["name", "=", "КП: базовий контур"]]`
   - очікувати кількість: 1

## Продажі р.1 · один прайс-лист

105. **створити** `product.pricelist` (тільки якщо немає `[["name", "=", "Прайс клієнта"]]`) → назвати `$прайс`
   - значення: `{"name": "Прайс клієнта"}`
106. **створити** `product.pricelist.item` (тільки якщо немає `[["pricelist_id", "in", "$прайс"]]`)
   - значення: `{"pricelist_id": "$прайс", "applied_on": "3_global", "compute_price": "percentage", "percent_price": 10}`
107. **перевірити** `product.pricelist.item` домен `[["pricelist_id", "in", "$прайс"]]`
   - очікувати щонайменше: 1

## Продажі р.1 · замовлення клієнта

108. **перевірити** `ir.model` домен `[["model", "=", "sale.order"]]`
   - очікувати кількість: 1

## Продажі р.1 · рахунок (стандартний бланк Odoo); акт виконаних робіт — власним бланком у позиції «Друковані форми»

109. **перевірити** `ir.actions.report` домен `[["model", "=", "account.move"]]`
   - очікувати щонайменше: 1

## Продажі р.1 · звіт за продажами

110. **перевірити** `ir.model` домен `[["model", "=", "sale.report"]]`
   - очікувати кількість: 1

## Продажі р.2 · додаткові поля в замовленні та КП

111. **знайти** `ir.model` домен `[["model", "=", "sale.order"]]` → назвати `$модель_поля`
112. **створити** `ir.model.fields` (тільки якщо немає `[["model", "=", "sale.order"], ["name", "=", "x_kanal_prodazhu"]]`) → назвати `$власне_поле`
   - значення: `{"name": "x_kanal_prodazhu", "field_description": "Канал продажу", "model_id": "$модель_поля", "model": "sale.order", "ttype": "char", "state": "manual"}`
113. **знайти** `ir.ui.view` домен `[["name", "=", "sale.order.form"], ["type", "=", "form"], ["inherit_id", "=", false]]` → назвати `$подання_батько`
114. **створити** `ir.ui.view` (тільки якщо немає `[["name", "=", "sale.order.x_kanal_prodazhu"]]`)
   - значення: `{"name": "sale.order.x_kanal_prodazhu", "model": "sale.order", "inherit_id": "$подання_батько", "arch": "<data><xpath expr=\"//sheet\" position=\"inside\"><group string=\"Додатково\"><field name=\"x_kanal_prodazhu\"/></group></xpath></data>"}`
115. **перевірити** `ir.model.fields` домен `[["model", "=", "sale.order"], ["name", "=", "x_kanal_prodazhu"]]`
   - очікувати кількість: 1

## Продажі р.2 · погодження КП у браузері за посиланням: клієнт приймає й підписує без реєстрації (штатний підпис у замовленні)

116. **знайти** `res.company` домен `[["id", "=", 1]]` → назвати `$компанія`
117. **записати** `res.company` → ціль `$компанія`
   - значення: `{"portal_confirmation_sign": true, "quotation_validity_days": 14}`
118. **перевірити** `res.company` → ціль `$компанія`
119. **перевірити** `ir.model` домен `[["model", "=", "sale.order"]]`

## Продажі р.3 · окреме робоче місце під сегмент: свій пункт меню, своє подання замовлень і свій фільтр

120. **створити** `ir.ui.view` (тільки якщо немає `[["name", "=", "sale.order.місце.Замовлення сегмента"]]`) → назвати `$подання_місця`
   - значення: `{"name": "sale.order.місце.Замовлення сегмента", "model": "sale.order", "type": "list", "arch": "<list string=\"Замовлення сегмента\"><field name=\"name\"/><field name=\"partner_id\"/><field name=\"date_order\"/><field name=\"amount_total\"/><field name=\"state\"/></list>"}`
121. **створити** `ir.actions.act_window` (тільки якщо немає `[["name", "=", "Замовлення сегмента"], ["res_model", "=", "sale.order"]]`) → назвати `$дія_місця`
   - значення: `{"name": "Замовлення сегмента", "res_model": "sale.order", "view_mode": "list,form", "view_id": "$подання_місця", "domain": "[('state','in',['sale','done'])]", "context": "{'group_by': ['partner_id']}"}`
122. **знайти_xmlid** → назвати `$меню_батько`
   - зовнішній id: `sale.sale_menu_root`
123. **знайти_xmlid** → назвати `$роль_місця`
   - зовнішній id: `sales_team.group_sale_salesman`
124. **створити** `ir.ui.menu` (тільки якщо немає `[["name", "=", "Замовлення сегмента"], ["parent_id", "=", "$меню_батько"]]`)
   - значення: `{"name": "Замовлення сегмента", "parent_id": "$меню_батько", "action": "$дія_місця", "group_ids": [[4, "$роль_місця"]], "sequence": 90}`
125. **створити** `ir.filters` (тільки якщо немає `[["name", "=", "Сегмент: підтверджені замовлення"], ["model_id", "=", "sale.order"]]`)
   - значення: `{"name": "Сегмент: підтверджені замовлення", "model_id": "sale.order", "domain": "[('state','in',['sale','done'])]", "context": "{}", "sort": "[]", "action_id": "$дія_місця"}`
126. **перевірити** `ir.ui.menu` домен `[["name", "=", "Замовлення сегмента"]]`
   - очікувати кількість: 1

## Продажі р.3 · кнопки узгодження в замовленні

127. **знайти** `ir.model` домен `[["model", "=", "sale.order"]]` → назвати `$модель_поля`
128. **створити** `ir.model.fields` (тільки якщо немає `[["model", "=", "sale.order"], ["name", "=", "x_visa_kerivnyka"]]`) → назвати `$власне_поле`
   - значення: `{"name": "x_visa_kerivnyka", "field_description": "Віза керівника", "model_id": "$модель_поля", "model": "sale.order", "ttype": "boolean", "state": "manual"}`
129. **знайти** `ir.ui.view` домен `[["name", "=", "sale.order.form"], ["type", "=", "form"], ["inherit_id", "=", false]]` → назвати `$подання_батько`
130. **створити** `ir.ui.view` (тільки якщо немає `[["name", "=", "sale.order.x_visa_kerivnyka"]]`)
   - значення: `{"name": "sale.order.x_visa_kerivnyka", "model": "sale.order", "inherit_id": "$подання_батько", "arch": "<data><xpath expr=\"//sheet\" position=\"inside\"><group string=\"Узгодження\"><field name=\"x_visa_kerivnyka\"/></group></xpath></data>"}`
131. **перевірити** `ir.model.fields` домен `[["model", "=", "sale.order"], ["name", "=", "x_visa_kerivnyka"]]`
   - очікувати кількість: 1
132. **знайти** `ir.model` домен `[["model", "=", "sale.order"]]` → назвати `$модель_кнопки`
133. **створити** `ir.actions.server` (тільки якщо немає `[["name", "=", "Поставити візу керівника"], ["model_id", "in", "$модель_кнопки"]]`)
   - значення: `{"name": "Поставити візу керівника", "model_id": "$модель_кнопки", "state": "object_write", "update_path": "x_visa_kerivnyka", "evaluation_type": "value", "update_boolean_value": "true", "binding_model_id": "$модель_кнопки", "binding_type": "action", "binding_view_types": "form,list"}`
134. **перевірити** `ir.actions.server` домен `[["name", "=", "Поставити візу керівника"], ["model_id", "in", "$модель_кнопки"]]`
   - очікувати кількість: 1

## Інтернет-магазин р.3 · окреме робоче місце під замовлення магазину: свій пункт меню, своє подання й свій фільтр

135. **створити** `ir.ui.view` (тільки якщо немає `[["name", "=", "sale.order.місце.Замовлення магазину"]]`) → назвати `$подання_місця`
   - значення: `{"name": "sale.order.місце.Замовлення магазину", "model": "sale.order", "type": "list", "arch": "<list string=\"Замовлення магазину\"><field name=\"name\"/><field name=\"partner_id\"/><field name=\"website_id\"/><field name=\"amount_total\"/><field name=\"state\"/></list>"}`
136. **створити** `ir.actions.act_window` (тільки якщо немає `[["name", "=", "Замовлення магазину"], ["res_model", "=", "sale.order"]]`) → назвати `$дія_місця`
   - значення: `{"name": "Замовлення магазину", "res_model": "sale.order", "view_mode": "list,form", "view_id": "$подання_місця", "domain": "[('website_id','!=',False)]", "context": "{'group_by': ['website_id']}"}`
137. **знайти_xmlid** → назвати `$меню_батько`
   - зовнішній id: `website.menu_website_configuration`
138. **знайти_xmlid** → назвати `$роль_місця`
   - зовнішній id: `website.group_website_restricted_editor`
139. **створити** `ir.ui.menu` (тільки якщо немає `[["name", "=", "Замовлення магазину"], ["parent_id", "=", "$меню_батько"]]`)
   - значення: `{"name": "Замовлення магазину", "parent_id": "$меню_батько", "action": "$дія_місця", "group_ids": [[4, "$роль_місця"]], "sequence": 90}`
140. **створити** `ir.filters` (тільки якщо немає `[["name", "=", "Замовлення з сайту"], ["model_id", "=", "sale.order"]]`)
   - значення: `{"name": "Замовлення з сайту", "model_id": "sale.order", "domain": "[('website_id','!=',False)]", "context": "{}", "sort": "[]", "action_id": "$дія_місця"}`
141. **перевірити** `ir.ui.menu` домен `[["name", "=", "Замовлення магазину"]]`
   - очікувати кількість: 1

## Інтернет-магазин р.3 · поля логістики в замовленні

142. **знайти** `ir.model` домен `[["model", "=", "sale.order"]]` → назвати `$модель_поля`
143. **створити** `ir.model.fields` (тільки якщо немає `[["model", "=", "sale.order"], ["name", "=", "x_logistyka_sposib"]]`) → назвати `$власне_поле`
   - значення: `{"name": "x_logistyka_sposib", "field_description": "Логістика: спосіб передачі", "model_id": "$модель_поля", "model": "sale.order", "ttype": "char", "state": "manual"}`
144. **знайти** `ir.ui.view` домен `[["name", "=", "sale.order.form"], ["type", "=", "form"], ["inherit_id", "=", false]]` → назвати `$подання_батько`
145. **створити** `ir.ui.view` (тільки якщо немає `[["name", "=", "sale.order.x_logistyka_sposib"]]`)
   - значення: `{"name": "sale.order.x_logistyka_sposib", "model": "sale.order", "inherit_id": "$подання_батько", "arch": "<data><xpath expr=\"//sheet\" position=\"inside\"><group string=\"Логістика\"><field name=\"x_logistyka_sposib\"/></group></xpath></data>"}`
146. **перевірити** `ir.model.fields` домен `[["model", "=", "sale.order"], ["name", "=", "x_logistyka_sposib"]]`
   - очікувати кількість: 1

## Точка продажу р.3 · окреме робоче місце для звірки змін: свій пункт меню, своє подання й свій фільтр

147. **створити** `ir.ui.view` (тільки якщо немає `[["name", "=", "pos.order.місце.Звірка змін"]]`) → назвати `$подання_місця`
   - значення: `{"name": "pos.order.місце.Звірка змін", "model": "pos.order", "type": "list", "arch": "<list string=\"Звірка змін\"><field name=\"name\"/><field name=\"session_id\"/><field name=\"date_order\"/><field name=\"amount_total\"/><field name=\"state\"/></list>"}`
148. **створити** `ir.actions.act_window` (тільки якщо немає `[["name", "=", "Звірка змін"], ["res_model", "=", "pos.order"]]`) → назвати `$дія_місця`
   - значення: `{"name": "Звірка змін", "res_model": "pos.order", "view_mode": "list,form", "view_id": "$подання_місця", "domain": "[('x_pos_zvireno_zminu','=',False)]", "context": "{'group_by': ['session_id']}"}`
149. **знайти_xmlid** → назвати `$меню_батько`
   - зовнішній id: `point_of_sale.menu_point_root`
150. **знайти_xmlid** → назвати `$роль_місця`
   - зовнішній id: `point_of_sale.group_pos_manager`
151. **створити** `ir.ui.menu` (тільки якщо немає `[["name", "=", "Звірка змін"], ["parent_id", "=", "$меню_батько"]]`)
   - значення: `{"name": "Звірка змін", "parent_id": "$меню_батько", "action": "$дія_місця", "group_ids": [[4, "$роль_місця"]], "sequence": 90}`
152. **створити** `ir.filters` (тільки якщо немає `[["name", "=", "Незвірені чеки зміни"], ["model_id", "=", "pos.order"]]`)
   - значення: `{"name": "Незвірені чеки зміни", "model_id": "pos.order", "domain": "[('x_pos_zvireno_zminu','=',False)]", "context": "{}", "sort": "[]", "action_id": "$дія_місця"}`
153. **перевірити** `ir.ui.menu` домен `[["name", "=", "Звірка змін"]]`
   - очікувати кількість: 1

## Закупівлі р.2 · поля умов постачання в замовленні

154. **знайти** `ir.model` домен `[["model", "=", "purchase.order"]]` → назвати `$модель_поля`
155. **створити** `ir.model.fields` (тільки якщо немає `[["model", "=", "purchase.order"], ["name", "=", "x_umovy_incoterms"]]`) → назвати `$власне_поле`
   - значення: `{"name": "x_umovy_incoterms", "field_description": "Умови постачання: базис", "model_id": "$модель_поля", "model": "purchase.order", "ttype": "char", "state": "manual"}`
156. **знайти** `ir.ui.view` домен `[["name", "=", "purchase.order.form"], ["type", "=", "form"], ["inherit_id", "=", false]]` → назвати `$подання_батько`
157. **створити** `ir.ui.view` (тільки якщо немає `[["name", "=", "purchase.order.x_umovy_incoterms"]]`)
   - значення: `{"name": "purchase.order.x_umovy_incoterms", "model": "purchase.order", "inherit_id": "$подання_батько", "arch": "<data><xpath expr=\"//sheet\" position=\"inside\"><group string=\"Умови постачання\"><field name=\"x_umovy_incoterms\"/></group></xpath></data>"}`
158. **перевірити** `ir.model.fields` домен `[["model", "=", "purchase.order"], ["name", "=", "x_umovy_incoterms"]]`
   - очікувати кількість: 1

## Закупівлі р.3 · кнопки узгодження заявки

159. **знайти** `ir.model` домен `[["model", "=", "purchase.order"]]` → назвати `$модель_поля`
160. **створити** `ir.model.fields` (тільки якщо немає `[["model", "=", "purchase.order"], ["name", "=", "x_visa_head"]]`) → назвати `$власне_поле`
   - значення: `{"name": "x_visa_head", "field_description": "Віза керівника закупівель", "model_id": "$модель_поля", "model": "purchase.order", "ttype": "boolean", "state": "manual"}`
161. **знайти** `ir.ui.view` домен `[["name", "=", "purchase.order.form"], ["type", "=", "form"], ["inherit_id", "=", false]]` → назвати `$подання_батько`
162. **створити** `ir.ui.view` (тільки якщо немає `[["name", "=", "purchase.order.x_visa_head"]]`)
   - значення: `{"name": "purchase.order.x_visa_head", "model": "purchase.order", "inherit_id": "$подання_батько", "arch": "<data><xpath expr=\"//sheet\" position=\"inside\"><group string=\"Узгодження\"><field name=\"x_visa_head\"/></group></xpath></data>"}`
163. **перевірити** `ir.model.fields` домен `[["model", "=", "purchase.order"], ["name", "=", "x_visa_head"]]`
   - очікувати кількість: 1
164. **знайти** `ir.model` домен `[["model", "=", "purchase.order"]]` → назвати `$модель_кнопки`
165. **створити** `ir.actions.server` (тільки якщо немає `[["name", "=", "Поставити візу керівника закупівель"], ["model_id", "in", "$модель_кнопки"]]`)
   - значення: `{"name": "Поставити візу керівника закупівель", "model_id": "$модель_кнопки", "state": "object_write", "update_path": "x_visa_head", "evaluation_type": "value", "update_boolean_value": "true", "binding_model_id": "$модель_кнопки", "binding_type": "action", "binding_view_types": "form,list"}`
166. **перевірити** `ir.actions.server` домен `[["name", "=", "Поставити візу керівника закупівель"], ["model_id", "in", "$модель_кнопки"]]`
   - очікувати кількість: 1
167. **знайти** `ir.model` домен `[["model", "=", "purchase.order"]]` → назвати `$модель_кнопки`
168. **знайти** `mail.activity.type` домен `[["name", "=", "To-Do"]]` → назвати `$тип_активності`
169. **створити** `ir.actions.server` (тільки якщо немає `[["name", "=", "Погодити з фінансами — завдання"], ["model_id", "in", "$модель_кнопки"]]`)
   - значення: `{"name": "Погодити з фінансами — завдання", "model_id": "$модель_кнопки", "state": "next_activity", "activity_type_id": "$тип_активності", "activity_summary": "Погодити закупку з фінансами", "activity_date_deadline_range": 1, "activity_date_deadline_range_type": "days", "activity_user_type": "generic", "activity_user_field_name": "user_id", "binding_model_id": "$модель_кнопки", "binding_type": "action", "binding_view_types": "form,list"}`
170. **перевірити** `ir.actions.server` домен `[["name", "=", "Погодити з фінансами — завдання"], ["model_id", "in", "$модель_кнопки"]]`
   - очікувати кількість: 1

## Проєкти й таймшити р.1 · проєкти й задачі зі стадіями

171. **створити** `project.project` (тільки якщо немає `[["name", "=", "Впровадження Odoo — ТОВ «Імпорт Один»"]]`) → назвати `$проєкт`
   - значення: `{"name": "Впровадження Odoo — ТОВ «Імпорт Один»", "allow_timesheets": true}`
172. **створити** `project.task.type` (тільки якщо немає `[["name", "=", "Зібрати вимоги"], ["project_ids", "in", "$проєкт"]]`)
   - значення: `{"name": "Зібрати вимоги", "sequence": 1, "project_ids": [[4, "$проєкт"]]}`
173. **створити** `project.task.type` (тільки якщо немає `[["name", "=", "Налаштування"], ["project_ids", "in", "$проєкт"]]`)
   - значення: `{"name": "Налаштування", "sequence": 2, "project_ids": [[4, "$проєкт"]]}`
174. **створити** `project.task.type` (тільки якщо немає `[["name", "=", "На перевірці у клієнта"], ["project_ids", "in", "$проєкт"]]`)
   - значення: `{"name": "На перевірці у клієнта", "sequence": 3, "project_ids": [[4, "$проєкт"]]}`
175. **створити** `project.task.type` (тільки якщо немає `[["name", "=", "Прийнято"], ["project_ids", "in", "$проєкт"]]`)
   - значення: `{"name": "Прийнято", "sequence": 4, "project_ids": [[4, "$проєкт"]]}`
176. **перевірити** `project.task.type` домен `[["project_ids", "in", "$проєкт"]]`
   - очікувати кількість: 4

## Проєкти й таймшити р.1 · виконавці та строки

177. **знайти** `project.project` домен `[["name", "=", "Впровадження Odoo — ТОВ «Імпорт Один»"]]` → назвати `$проєкт`
178. **знайти** `res.users` домен `[["login", "in", ["sales1@example.com"]]]` → назвати `$виконавці`
179. **створити** `project.task` (тільки якщо немає `[["name", "=", "Заповнити анкету й зібрати дані"], ["project_id", "in", "$проєкт"]]`) → назвати `$задача`
   - значення: `{"name": "Заповнити анкету й зібрати дані", "project_id": "$проєкт", "user_ids": [[6, false, "$виконавці"]], "date_deadline": "2026-09-30", "allocated_hours": 8}`
180. **перевірити** `project.task` домен `[["project_id", "in", "$проєкт"]]`
   - очікувати щонайменше: 1

## Проєкти й таймшити р.1 · картки виконавців-співробітників (без кадрового обліку)

181. **створити** `hr.employee` (тільки якщо немає `[["name", "=", "Аналітик Приклад"]]`)
   - значення: `{"name": "Аналітик Приклад", "hourly_cost": 250}`
182. **перевірити** `hr.employee` домен `[]`
   - очікувати щонайменше: 1

## Проєкти й таймшити р.1 · списання годин на задачі

183. **знайти** `project.project` домен `[["name", "=", "Впровадження Odoo — ТОВ «Імпорт Один»"]]` → назвати `$проєкт`
184. **знайти** `project.task` домен `[["name", "=", "Заповнити анкету й зібрати дані"], ["project_id", "in", "$проєкт"]]` → назвати `$задача`
185. **знайти** `hr.employee` домен `[["name", "=", "Аналітик Приклад"]]` → назвати `$співробітник`
186. **створити** `account.analytic.line` (тільки якщо немає `[["task_id", "in", "$задача"], ["name", "=", "Збір вимог із клієнтом"]]`) → назвати `$таймшит`
   - значення: `{"name": "Збір вимог із клієнтом", "project_id": "$проєкт", "task_id": "$задача", "employee_id": "$співробітник", "unit_amount": 3.5}`
187. **перевірити** `project.task` → ціль `$задача`
   - очікувати: `{"effective_hours": 3.5, "remaining_hours": 4.5}`

## Проєкти й таймшити р.1 · звіт за витраченим часом

188. **знайти** `project.project` домен `[["name", "=", "Впровадження Odoo — ТОВ «Імпорт Один»"]]` → назвати `$проєкт`
189. **перевірити** `account.analytic.line` домен `[["project_id", "in", "$проєкт"]]`
   - очікувати щонайменше: 1

## Проєкти й таймшити р.1 · статус проєктів на дашборді

190. **перевірити** `project.project` домен `[["name", "=", "Впровадження Odoo — ТОВ «Імпорт Один»"]]`
   - очікувати кількість: 1

## Проєкти й таймшити р.2 · власні поля в проєкті й задачі

191. **властивість** `project.task`

## Проєкти й таймшити р.3 · окреме робоче місце під тип проєктів: свій пункт меню, своє подання й свій фільтр

192. **створити** `ir.ui.view` (тільки якщо немає `[["name", "=", "project.task.місце.Задачі за типом проєктів"]]`) → назвати `$подання_місця`
   - значення: `{"name": "project.task.місце.Задачі за типом проєктів", "model": "project.task", "type": "list", "arch": "<list string=\"Задачі за типом проєктів\"><field name=\"name\"/><field name=\"project_id\"/><field name=\"user_ids\"/><field name=\"stage_id\"/><field name=\"date_deadline\"/></list>"}`
193. **створити** `ir.actions.act_window` (тільки якщо немає `[["name", "=", "Задачі за типом проєктів"], ["res_model", "=", "project.task"]]`) → назвати `$дія_місця`
   - значення: `{"name": "Задачі за типом проєктів", "res_model": "project.task", "view_mode": "list,form", "view_id": "$подання_місця", "domain": "[('user_ids','=',False)]", "context": "{'group_by': ['project_id']}"}`
194. **знайти_xmlid** → назвати `$меню_батько`
   - зовнішній id: `project.menu_main_pm`
195. **знайти_xmlid** → назвати `$роль_місця`
   - зовнішній id: `project.group_project_user`
196. **створити** `ir.ui.menu` (тільки якщо немає `[["name", "=", "Задачі за типом проєктів"], ["parent_id", "=", "$меню_батько"]]`)
   - значення: `{"name": "Задачі за типом проєктів", "parent_id": "$меню_батько", "action": "$дія_місця", "group_ids": [[4, "$роль_місця"]], "sequence": 90}`
197. **створити** `ir.filters` (тільки якщо немає `[["name", "=", "Задачі без виконавця"], ["model_id", "=", "project.task"]]`)
   - значення: `{"name": "Задачі без виконавця", "model_id": "project.task", "domain": "[('user_ids','=',False)]", "context": "{}", "sort": "[]", "action_id": "$дія_місця"}`
198. **перевірити** `ir.ui.menu` домен `[["name", "=", "Задачі за типом проєктів"]]`
   - очікувати кількість: 1

## Проєкти й таймшити р.3 · кнопки переходу між стадіями

199. **знайти** `ir.model` домен `[["model", "=", "project.task"]]` → назвати `$модель_кнопки`
200. **знайти** `project.task.type` домен `[["name", "=", "Налаштування"]]` → назвати `$стадія`
201. **створити** `ir.actions.server` (тільки якщо немає `[["name", "=", "→ Налаштування"], ["model_id", "in", "$модель_кнопки"]]`)
   - значення: `{"name": "→ Налаштування", "model_id": "$модель_кнопки", "state": "object_write", "update_path": "stage_id", "evaluation_type": "value", "resource_ref": "project.task.type,$стадія", "binding_model_id": "$модель_кнопки", "binding_type": "action", "binding_view_types": "form,list"}`
202. **перевірити** `ir.actions.server` домен `[["name", "=", "→ Налаштування"], ["model_id", "in", "$модель_кнопки"]]`
   - очікувати кількість: 1
203. **знайти** `ir.model` домен `[["model", "=", "project.task"]]` → назвати `$модель_кнопки`
204. **знайти** `project.task.type` домен `[["name", "=", "На перевірці у клієнта"]]` → назвати `$стадія`
205. **створити** `ir.actions.server` (тільки якщо немає `[["name", "=", "→ На перевірці у клієнта"], ["model_id", "in", "$модель_кнопки"]]`)
   - значення: `{"name": "→ На перевірці у клієнта", "model_id": "$модель_кнопки", "state": "object_write", "update_path": "stage_id", "evaluation_type": "value", "resource_ref": "project.task.type,$стадія", "binding_model_id": "$модель_кнопки", "binding_type": "action", "binding_view_types": "form,list"}`
206. **перевірити** `ir.actions.server` домен `[["name", "=", "→ На перевірці у клієнта"], ["model_id", "in", "$модель_кнопки"]]`
   - очікувати кількість: 1

## Виїзні роботи р.2 · карта виїздів за день у порядку планового часу (лінія маршруту — за наявності токена MapBox у клієнта)

207. **створити** `ir.ui.view` (тільки якщо немає `[["name", "=", "project.task.місце.Карта виїздів на сьогодні"]]`) → назвати `$подання_місця`
   - значення: `{"name": "project.task.місце.Карта виїздів на сьогодні", "model": "project.task", "type": "list", "arch": "<list string=\"Карта виїздів на сьогодні\"><field name=\"name\"/><field name=\"partner_id\"/><field name=\"planned_date_begin\"/><field name=\"user_ids\"/></list>"}`
208. **створити** `ir.actions.act_window` (тільки якщо немає `[["name", "=", "Карта виїздів на сьогодні"], ["res_model", "=", "project.task"]]`) → назвати `$дія_місця`
   - значення: `{"name": "Карта виїздів на сьогодні", "res_model": "project.task", "view_mode": "list,form", "view_id": "$подання_місця", "domain": "[('planned_date_begin','!=',False)]", "context": "{'group_by': ['user_ids']}"}`
209. **знайти_xmlid** → назвати `$меню_батько`
   - зовнішній id: `industry_fsm.fsm_menu_root`
210. **знайти_xmlid** → назвати `$роль_місця`
   - зовнішній id: `industry_fsm.group_fsm_user`
211. **створити** `ir.ui.menu` (тільки якщо немає `[["name", "=", "Карта виїздів на сьогодні"], ["parent_id", "=", "$меню_батько"]]`)
   - значення: `{"name": "Карта виїздів на сьогодні", "parent_id": "$меню_батько", "action": "$дія_місця", "group_ids": [[4, "$роль_місця"]], "sequence": 90}`
212. **створити** `ir.filters` (тільки якщо немає `[["name", "=", "Виїзди дня"], ["model_id", "=", "project.task"]]`)
   - значення: `{"name": "Виїзди дня", "model_id": "project.task", "domain": "[('planned_date_begin','!=',False)]", "context": "{}", "sort": "[]", "action_id": "$дія_місця"}`
213. **перевірити** `ir.ui.menu` домен `[["name", "=", "Карта виїздів на сьогодні"]]`
   - очікувати кількість: 1

## Виїзні роботи р.2 · поля наряду під тип роботи

214. **властивість** `project.task`

## Планування р.3 · окреме робоче місце керівника: свій пункт меню, своє подання завантаження й свій фільтр

215. **створити** `ir.ui.view` (тільки якщо немає `[["name", "=", "planning.slot.місце.Завантаження команди"]]`) → назвати `$подання_місця`
   - значення: `{"name": "planning.slot.місце.Завантаження команди", "model": "planning.slot", "type": "list", "arch": "<list string=\"Завантаження команди\"><field name=\"resource_id\"/><field name=\"role_id\"/><field name=\"start_datetime\"/><field name=\"end_datetime\"/><field name=\"allocated_hours\" sum=\"Годин\"/></list>"}`
216. **створити** `ir.actions.act_window` (тільки якщо немає `[["name", "=", "Завантаження команди"], ["res_model", "=", "planning.slot"]]`) → назвати `$дія_місця`
   - значення: `{"name": "Завантаження команди", "res_model": "planning.slot", "view_mode": "list,form", "view_id": "$подання_місця", "domain": "[('resource_id','!=',False)]", "context": "{'group_by': ['role_id']}"}`
217. **знайти_xmlid** → назвати `$меню_батько`
   - зовнішній id: `planning.planning_menu_root`
218. **знайти_xmlid** → назвати `$роль_місця`
   - зовнішній id: `planning.group_planning_manager`
219. **створити** `ir.ui.menu` (тільки якщо немає `[["name", "=", "Завантаження команди"], ["parent_id", "=", "$меню_батько"]]`)
   - значення: `{"name": "Завантаження команди", "parent_id": "$меню_батько", "action": "$дія_місця", "group_ids": [[4, "$роль_місця"]], "sequence": 90}`
220. **створити** `ir.filters` (тільки якщо немає `[["name", "=", "Планові години по ролях"], ["model_id", "=", "planning.slot"]]`)
   - значення: `{"name": "Планові години по ролях", "model_id": "planning.slot", "domain": "[('resource_id','!=',False)]", "context": "{}", "sort": "[]", "action_id": "$дія_місця"}`
221. **перевірити** `ir.ui.menu` домен `[["name", "=", "Завантаження команди"]]`
   - очікувати кількість: 1

## Сервісні заявки р.3 · окреме робоче місце команди: свій пункт меню, своє подання черги й свій фільтр

222. **створити** `ir.ui.view` (тільки якщо немає `[["name", "=", "helpdesk.ticket.місце.Черга команди"]]`) → назвати `$подання_місця`
   - значення: `{"name": "helpdesk.ticket.місце.Черга команди", "model": "helpdesk.ticket", "type": "list", "arch": "<list string=\"Черга команди\"><field name=\"name\"/><field name=\"partner_id\"/><field name=\"team_id\"/><field name=\"user_id\"/><field name=\"stage_id\"/></list>"}`
223. **створити** `ir.actions.act_window` (тільки якщо немає `[["name", "=", "Черга команди"], ["res_model", "=", "helpdesk.ticket"]]`) → назвати `$дія_місця`
   - значення: `{"name": "Черга команди", "res_model": "helpdesk.ticket", "view_mode": "list,form", "view_id": "$подання_місця", "domain": "[('stage_id.fold','=',False)]", "context": "{'group_by': ['team_id']}"}`
224. **знайти_xmlid** → назвати `$меню_батько`
   - зовнішній id: `helpdesk.menu_helpdesk_root`
225. **знайти_xmlid** → назвати `$роль_місця`
   - зовнішній id: `helpdesk.group_helpdesk_user`
226. **створити** `ir.ui.menu` (тільки якщо немає `[["name", "=", "Черга команди"], ["parent_id", "=", "$меню_батько"]]`)
   - значення: `{"name": "Черга команди", "parent_id": "$меню_батько", "action": "$дія_місця", "group_ids": [[4, "$роль_місця"]], "sequence": 90}`
227. **створити** `ir.filters` (тільки якщо немає `[["name", "=", "Заявки без відповідального"], ["model_id", "=", "helpdesk.ticket"]]`)
   - значення: `{"name": "Заявки без відповідального", "model_id": "helpdesk.ticket", "domain": "[('stage_id.fold','=',False)]", "context": "{}", "sort": "[]", "action_id": "$дія_місця"}`
228. **перевірити** `ir.ui.menu` домен `[["name", "=", "Черга команди"]]`
   - очікувати кількість: 1

## Сервісні заявки р.3 · кнопки типових дій у заявці

229. **знайти** `ir.model` домен `[["model", "=", "helpdesk.ticket"]]` → назвати `$модель_кнопки`
230. **знайти** `helpdesk.stage` домен `[["name", "=", "In Progress"]]` → назвати `$стадія`
231. **створити** `ir.actions.server` (тільки якщо немає `[["name", "=", "→ У роботі"], ["model_id", "in", "$модель_кнопки"]]`)
   - значення: `{"name": "→ У роботі", "model_id": "$модель_кнопки", "state": "object_write", "update_path": "stage_id", "evaluation_type": "value", "resource_ref": "helpdesk.stage,$стадія", "binding_model_id": "$модель_кнопки", "binding_type": "action", "binding_view_types": "form,list"}`
232. **перевірити** `ir.actions.server` домен `[["name", "=", "→ У роботі"], ["model_id", "in", "$модель_кнопки"]]`
   - очікувати кількість: 1
233. **знайти** `ir.model` домен `[["model", "=", "helpdesk.ticket"]]` → назвати `$модель_кнопки`
234. **знайти** `helpdesk.stage` домен `[["name", "=", "Solved"]]` → назвати `$стадія`
235. **створити** `ir.actions.server` (тільки якщо немає `[["name", "=", "→ Вирішено"], ["model_id", "in", "$модель_кнопки"]]`)
   - значення: `{"name": "→ Вирішено", "model_id": "$модель_кнопки", "state": "object_write", "update_path": "stage_id", "evaluation_type": "value", "resource_ref": "helpdesk.stage,$стадія", "binding_model_id": "$модель_кнопки", "binding_type": "action", "binding_view_types": "form,list"}`
236. **перевірити** `ir.actions.server` домен `[["name", "=", "→ Вирішено"], ["model_id", "in", "$модель_кнопки"]]`
   - очікувати кількість: 1

## Підписки р.1 · договір з клієнтом

237. **створити** `sale.subscription.plan` (тільки якщо немає `[["name", "=", "Місячний"]]`) → назвати `$тариф`
   - значення: `{"name": "Місячний", "billing_period_value": 1, "billing_period_unit": "month", "user_closable_options": "end_of_period"}`
238. **перевірити** `sale.subscription.plan` → ціль `$тариф`
239. **знайти** `product.template` домен `[["name", "=", "Обслуговування кавомашини — підписка"]]` → назвати `$товар_підписки`
240. **записати** `product.template` → ціль `$товар_підписки`
   - значення: `{"recurring_invoice": true}`
241. **перевірити** `product.template` → ціль `$товар_підписки`

## Підписки р.1 · регулярний рахунок

242. **знайти** `ir.cron` домен `[["code", "=", "model._cron_recurring_create_invoice()"]]` → назвати `$розписання_рахунків`
243. **перевірити** `ir.cron` → ціль `$розписання_рахунків`
244. **перевірити** `ir.model` домен `[["model", "=", "account.move"]]`

## Підписки р.1 · звіт за активними підписками

245. **знайти_xmlid** → назвати `$роль_користувач`
   - зовнішній id: `base.group_user`
246. **створити** `ir.filters` (тільки якщо немає `[["name", "=", "Активні підписки"], ["model_id", "=", "sale.order"]]`) → назвати `$фільтр_підписок`
   - значення: `{"name": "Активні підписки", "model_id": "sale.order", "domain": "[('subscription_state','=','3_progress')]", "context": "{'group_by': ['plan_id']}", "user_ids": []}`
247. **перевірити** `ir.filters` → ціль `$фільтр_підписок`
248. **перевірити** `ir.model` домен `[["model", "=", "sale.order"]]`

## Підписки р.2 · поля тарифу в договорі

249. **знайти** `ir.model` домен `[["model", "=", "sale.order"]]` → назвати `$модель_поля`
250. **створити** `ir.model.fields` (тільки якщо немає `[["model", "=", "sale.order"], ["name", "=", "x_taryf_pidpysky"]]`) → назвати `$власне_поле`
   - значення: `{"name": "x_taryf_pidpysky", "field_description": "Тариф підписки", "model_id": "$модель_поля", "model": "sale.order", "ttype": "char", "state": "manual"}`
251. **знайти** `ir.ui.view` домен `[["name", "=", "sale.order.form"], ["type", "=", "form"], ["inherit_id", "=", false]]` → назвати `$подання_батько`
252. **створити** `ir.ui.view` (тільки якщо немає `[["name", "=", "sale.order.x_taryf_pidpysky"]]`)
   - значення: `{"name": "sale.order.x_taryf_pidpysky", "model": "sale.order", "inherit_id": "$подання_батько", "arch": "<data><xpath expr=\"//sheet\" position=\"inside\"><group string=\"Тариф\"><field name=\"x_taryf_pidpysky\"/></group></xpath></data>"}`
253. **перевірити** `ir.model.fields` домен `[["model", "=", "sale.order"], ["name", "=", "x_taryf_pidpysky"]]`
   - очікувати кількість: 1

## Підписки р.3 · розширення (upsell) з пропорційним донарахуванням; зменшення — без повернення коштів

254. **перевірити** `ir.model` домен `[["model", "=", "sale.order"]]`

## Підписки р.3 · аналітика Retention і MRR; churn у відсотках — окремий дашборд

255. **знайти** `ir.cron` домен `[["code", "=", "model._cron_update_kpi()"]]` → назвати `$розписання_kpi`
256. **перевірити** `ir.cron` → ціль `$розписання_kpi`
257. **перевірити** `ir.model` домен `[["model", "=", "sale.order"]]`

## Оренда р.1 · видача й повернення

258. **перевірити** `ir.model` домен `[["model", "=", "sale.order"]]`

## Оренда р.1 · договір і рахунок

259. **перевірити** `ir.model` домен `[["model", "=", "account.move"]]`

## Оренда р.2 · бронювання наперед

260. **знайти** `res.company` домен `[["id", "=", 1]]` → назвати `$компанія`
261. **записати** `res.company` → ціль `$компанія`
   - значення: `{"renting_minimal_time_duration": 1, "renting_minimal_time_unit": "day", "renting_forbidden_sun": true}`
262. **перевірити** `res.company` → ціль `$компанія`

## Оренда р.2 · поля стану й комплектації об’єкта

263. **створити** `product.category` (тільки якщо немає `[["name", "=", "Оренда обладнання"]]`) → назвати `$категорія_оренди`
   - значення: `{"name": "Оренда обладнання"}`
264. **перевірити** `product.category` → ціль `$категорія_оренди`
265. **властивість** `product.template`

## Обслуговування обладнання р.2 · поля обладнання під тип

266. **створити** `maintenance.equipment.category` (тільки якщо немає `[["name", "=", "Кавомашини"]]`) → назвати `$категорія_обладнання`
   - значення: `{"name": "Кавомашини"}`
267. **перевірити** `maintenance.equipment.category` → ціль `$категорія_обладнання`
268. **властивість** `maintenance.equipment`

## Обслуговування обладнання р.3 · подання регламентів і кнопки закриття

269. **створити** `ir.ui.view` (тільки якщо немає `[["name", "=", "maintenance.request.місце.Регламенти й заявки"]]`) → назвати `$подання_місця`
   - значення: `{"name": "maintenance.request.місце.Регламенти й заявки", "model": "maintenance.request", "type": "list", "arch": "<list string=\"Регламенти й заявки\"><field name=\"name\"/><field name=\"equipment_id\"/><field name=\"request_date\"/><field name=\"stage_id\"/></list>"}`
270. **створити** `ir.actions.act_window` (тільки якщо немає `[["name", "=", "Регламенти й заявки"], ["res_model", "=", "maintenance.request"]]`) → назвати `$дія_місця`
   - значення: `{"name": "Регламенти й заявки", "res_model": "maintenance.request", "view_mode": "list,form", "view_id": "$подання_місця", "domain": "[('maintenance_type','=','preventive')]", "context": "{'group_by': ['stage_id']}"}`
271. **знайти_xmlid** → назвати `$меню_батько`
   - зовнішній id: `maintenance.menu_maintenance_title`
272. **знайти_xmlid** → назвати `$роль_місця`
   - зовнішній id: `maintenance.group_equipment_manager`
273. **створити** `ir.ui.menu` (тільки якщо немає `[["name", "=", "Регламенти й заявки"], ["parent_id", "=", "$меню_батько"]]`)
   - значення: `{"name": "Регламенти й заявки", "parent_id": "$меню_батько", "action": "$дія_місця", "group_ids": [[4, "$роль_місця"]], "sequence": 90}`
274. **створити** `ir.filters` (тільки якщо немає `[["name", "=", "Регламенти на цей місяць"], ["model_id", "=", "maintenance.request"]]`)
   - значення: `{"name": "Регламенти на цей місяць", "model_id": "maintenance.request", "domain": "[('maintenance_type','=','preventive')]", "context": "{}", "sort": "[]", "action_id": "$дія_місця"}`
275. **перевірити** `ir.ui.menu` домен `[["name", "=", "Регламенти й заявки"]]`
   - очікувати кількість: 1
276. **знайти** `ir.model` домен `[["model", "=", "maintenance.request"]]` → назвати `$модель_кнопки`
277. **знайти** `maintenance.stage` домен `[["name", "=", "Repaired"]]` → назвати `$стадія`
278. **створити** `ir.actions.server` (тільки якщо немає `[["name", "=", "Закрити заявку — відремонтовано"], ["model_id", "in", "$модель_кнопки"]]`)
   - значення: `{"name": "Закрити заявку — відремонтовано", "model_id": "$модель_кнопки", "state": "object_write", "update_path": "stage_id", "evaluation_type": "value", "resource_ref": "maintenance.stage,$стадія", "binding_model_id": "$модель_кнопки", "binding_type": "action", "binding_view_types": "form,list"}`
279. **перевірити** `ir.actions.server` домен `[["name", "=", "Закрити заявку — відремонтовано"], ["model_id", "in", "$модель_кнопки"]]`
   - очікувати кількість: 1

## Витрати співробітників р.2 · поля категорій і лімітів

280. **властивість** `product.template`

## Витрати співробітників р.3 · кнопки узгодження у витраті

281. **знайти** `ir.model` домен `[["model", "=", "hr.expense"]]` → назвати `$модель_поля`
282. **створити** `ir.model.fields` (тільки якщо немає `[["model", "=", "hr.expense"], ["name", "=", "x_over_limit"]]`) → назвати `$власне_поле`
   - значення: `{"name": "x_over_limit", "field_description": "Понад ліміт категорії — додаткове узгодження", "model_id": "$модель_поля", "model": "hr.expense", "ttype": "boolean", "state": "manual"}`
283. **знайти** `ir.ui.view` домен `[["name", "=", "hr.expense.view.form"], ["type", "=", "form"], ["inherit_id", "=", false]]` → назвати `$подання_батько`
284. **створити** `ir.ui.view` (тільки якщо немає `[["name", "=", "hr.expense.x_over_limit"]]`)
   - значення: `{"name": "hr.expense.x_over_limit", "model": "hr.expense", "inherit_id": "$подання_батько", "arch": "<data><xpath expr=\"//sheet\" position=\"inside\"><group string=\"Узгодження\"><field name=\"x_over_limit\"/></group></xpath></data>"}`
285. **перевірити** `ir.model.fields` домен `[["model", "=", "hr.expense"], ["name", "=", "x_over_limit"]]`
   - очікувати кількість: 1
286. **знайти** `ir.model` домен `[["model", "=", "hr.expense"]]` → назвати `$модель_кнопки`
287. **створити** `ir.actions.server` (тільки якщо немає `[["name", "=", "Позначити «понад ліміт»"], ["model_id", "in", "$модель_кнопки"]]`)
   - значення: `{"name": "Позначити «понад ліміт»", "model_id": "$модель_кнопки", "state": "object_write", "update_path": "x_over_limit", "evaluation_type": "value", "update_boolean_value": "true", "binding_model_id": "$модель_кнопки", "binding_type": "action", "binding_view_types": "form,list"}`
288. **перевірити** `ir.actions.server` домен `[["name", "=", "Позначити «понад ліміт»"], ["model_id", "in", "$модель_кнопки"]]`
   - очікувати кількість: 1
289. **знайти** `ir.model` домен `[["model", "=", "hr.expense"]]` → назвати `$модель_кнопки`
290. **знайти** `mail.activity.type` домен `[["name", "=", "Expense Approval"]]` → назвати `$тип_активності`
291. **створити** `ir.actions.server` (тільки якщо немає `[["name", "=", "Погодити з керівником — завдання на завтра"], ["model_id", "in", "$модель_кнопки"]]`)
   - значення: `{"name": "Погодити з керівником — завдання на завтра", "model_id": "$модель_кнопки", "state": "next_activity", "activity_type_id": "$тип_активності", "activity_summary": "Погодити витрату", "activity_date_deadline_range": 1, "activity_date_deadline_range_type": "days", "activity_user_type": "generic", "activity_user_field_name": "employee_id", "binding_model_id": "$модель_кнопки", "binding_type": "action", "binding_view_types": "form,list"}`
292. **перевірити** `ir.actions.server` домен `[["name", "=", "Погодити з керівником — завдання на завтра"], ["model_id", "in", "$модель_кнопки"]]`
   - очікувати кількість: 1

## Персонал і відпустки р.3 · окреме робоче місце кадровика: свій пункт меню, своє подання й свій фільтр

293. **створити** `ir.ui.view` (тільки якщо немає `[["name", "=", "hr.employee.місце.Робоче місце кадровика"]]`) → назвати `$подання_місця`
   - значення: `{"name": "hr.employee.місце.Робоче місце кадровика", "model": "hr.employee", "type": "list", "arch": "<list string=\"Робоче місце кадровика\"><field name=\"name\"/><field name=\"department_id\"/><field name=\"job_title\"/><field name=\"resource_calendar_id\"/><field name=\"work_email\"/></list>"}`
294. **створити** `ir.actions.act_window` (тільки якщо немає `[["name", "=", "Робоче місце кадровика"], ["res_model", "=", "hr.employee"]]`) → назвати `$дія_місця`
   - значення: `{"name": "Робоче місце кадровика", "res_model": "hr.employee", "view_mode": "list,form", "view_id": "$подання_місця", "domain": "[('active','=',True)]", "context": "{'group_by': ['department_id']}"}`
295. **знайти_xmlid** → назвати `$меню_батько`
   - зовнішній id: `hr.menu_hr_root`
296. **знайти_xmlid** → назвати `$роль_місця`
   - зовнішній id: `hr.group_hr_user`
297. **створити** `ir.ui.menu` (тільки якщо немає `[["name", "=", "Робоче місце кадровика"], ["parent_id", "=", "$меню_батько"]]`)
   - значення: `{"name": "Робоче місце кадровика", "parent_id": "$меню_батько", "action": "$дія_місця", "group_ids": [[4, "$роль_місця"]], "sequence": 90}`
298. **створити** `ir.filters` (тільки якщо немає `[["name", "=", "Співробітники за підрозділами"], ["model_id", "=", "hr.employee"]]`)
   - значення: `{"name": "Співробітники за підрозділами", "model_id": "hr.employee", "domain": "[('active','=',True)]", "context": "{}", "sort": "[]", "action_id": "$дія_місця"}`
299. **перевірити** `ir.ui.menu` домен `[["name", "=", "Робоче місце кадровика"]]`
   - очікувати кількість: 1

## Документи й підписи р.2 · поля типу документа

300. **знайти** `ir.model` домен `[["model", "=", "documents.document"]]` → назвати `$модель_документа`
301. **створити** `ir.model.fields` (тільки якщо немає `[["model", "=", "documents.document"], ["name", "=", "x_typ_dokumenta"]]`) → назвати `$поле_типу`
   - значення: `{"name": "x_typ_dokumenta", "field_description": "Тип документа", "model_id": "$модель_документа", "model": "documents.document", "ttype": "char", "state": "manual"}`
302. **знайти** `ir.ui.view` домен `[["model", "=", "documents.document"], ["type", "=", "list"], ["inherit_id", "=", false]]` → назвати `$перелік_документів`
303. **створити** `ir.ui.view` (тільки якщо немає `[["name", "=", "documents.document.x_typ_dokumenta"]]`)
   - значення: `{"name": "documents.document.x_typ_dokumenta", "model": "documents.document", "inherit_id": "$перелік_документів", "arch": "<data><xpath expr=\"//list\" position=\"inside\"><field name=\"x_typ_dokumenta\"/></xpath></data>"}`
304. **перевірити** `ir.model.fields` → ціль `$поле_типу`

## Документи й підписи р.3 · подання маршрутів і кнопки етапів

305. **створити** `ir.ui.view` (тільки якщо немає `[["name", "=", "sign.request.місце.Маршрути підписання"]]`) → назвати `$подання_місця`
   - значення: `{"name": "sign.request.місце.Маршрути підписання", "model": "sign.request", "type": "list", "arch": "<list string=\"Маршрути підписання\"><field name=\"reference\"/><field name=\"state\"/><field name=\"create_date\"/></list>"}`
306. **створити** `ir.actions.act_window` (тільки якщо немає `[["name", "=", "Маршрути підписання"], ["res_model", "=", "sign.request"]]`) → назвати `$дія_місця`
   - значення: `{"name": "Маршрути підписання", "res_model": "sign.request", "view_mode": "list,form", "view_id": "$подання_місця", "domain": "[('state','!=','signed')]", "context": "{'group_by': ['state']}"}`
307. **знайти_xmlid** → назвати `$меню_батько`
   - зовнішній id: `sign.menu_document`
308. **знайти_xmlid** → назвати `$роль_місця`
   - зовнішній id: `sign.group_sign_user`
309. **створити** `ir.ui.menu` (тільки якщо немає `[["name", "=", "Маршрути підписання"], ["parent_id", "=", "$меню_батько"]]`)
   - значення: `{"name": "Маршрути підписання", "parent_id": "$меню_батько", "action": "$дія_місця", "group_ids": [[4, "$роль_місця"]], "sequence": 90}`
310. **створити** `ir.filters` (тільки якщо немає `[["name", "=", "Непідписані маршрути"], ["model_id", "=", "sign.request"]]`)
   - значення: `{"name": "Непідписані маршрути", "model_id": "sign.request", "domain": "[('state','!=','signed')]", "context": "{}", "sort": "[]", "action_id": "$дія_місця"}`
311. **перевірити** `ir.ui.menu` домен `[["name", "=", "Маршрути підписання"]]`
   - очікувати кількість: 1

## Дашборди й звіти р.3 · окремі робочі місця під ролі: свій пункт меню, своє подання й свій фільтр

312. **створити** `ir.ui.view` (тільки якщо немає `[["name", "=", "crm.lead.місце.Воронка керівника"]]`) → назвати `$подання_місця`
   - значення: `{"name": "crm.lead.місце.Воронка керівника", "model": "crm.lead", "type": "list", "arch": "<list string=\"$елемент.назва\"><field name=\"display_name\"/></list>"}`
313. **створити** `ir.actions.act_window` (тільки якщо немає `[["name", "=", "Воронка керівника"], ["res_model", "=", "crm.lead"]]`) → назвати `$дія_місця`
   - значення: `{"name": "Воронка керівника", "res_model": "crm.lead", "view_mode": "list,form", "view_id": "$подання_місця", "domain": "[('stage_id.is_won','=',False)]", "context": "{}"}`
314. **знайти_xmlid** → назвати `$меню_батько`
   - зовнішній id: `crm.crm_menu_root`
315. **знайти_xmlid** → назвати `$роль_місця`
   - зовнішній id: `sales_team.group_sale_salesman_all_leads`
316. **створити** `ir.ui.menu` (тільки якщо немає `[["name", "=", "Воронка керівника"], ["parent_id", "=", "$меню_батько"]]`)
   - значення: `{"name": "Воронка керівника", "parent_id": "$меню_батько", "action": "$дія_місця", "group_ids": [[4, "$роль_місця"]], "sequence": 90}`
317. **створити** `ir.filters` (тільки якщо немає `[["name", "=", "Воронка керівника"], ["model_id", "=", "crm.lead"]]`)
   - значення: `{"name": "Воронка керівника", "model_id": "crm.lead", "domain": "[('stage_id.is_won','=',False)]", "context": "{}", "sort": "[]", "action_id": "$дія_місця"}`
318. **перевірити** `ir.ui.menu` домен `[["name", "=", "Воронка керівника"]]`
   - очікувати кількість: 1
319. **створити** `ir.ui.view` (тільки якщо немає `[["name", "=", "stock.picking.місце.Операції комірника"]]`) → назвати `$подання_місця`
   - значення: `{"name": "stock.picking.місце.Операції комірника", "model": "stock.picking", "type": "list", "arch": "<list string=\"$елемент.назва\"><field name=\"display_name\"/></list>"}`
320. **створити** `ir.actions.act_window` (тільки якщо немає `[["name", "=", "Операції комірника"], ["res_model", "=", "stock.picking"]]`) → назвати `$дія_місця`
   - значення: `{"name": "Операції комірника", "res_model": "stock.picking", "view_mode": "list,form", "view_id": "$подання_місця", "domain": "[('state','in',['assigned','confirmed'])]", "context": "{}"}`
321. **знайти_xmlid** → назвати `$меню_батько`
   - зовнішній id: `stock.menu_stock_root`
322. **знайти_xmlid** → назвати `$роль_місця`
   - зовнішній id: `stock.group_stock_user`
323. **створити** `ir.ui.menu` (тільки якщо немає `[["name", "=", "Операції комірника"], ["parent_id", "=", "$меню_батько"]]`)
   - значення: `{"name": "Операції комірника", "parent_id": "$меню_батько", "action": "$дія_місця", "group_ids": [[4, "$роль_місця"]], "sequence": 90}`
324. **створити** `ir.filters` (тільки якщо немає `[["name", "=", "Операції комірника"], ["model_id", "=", "stock.picking"]]`)
   - значення: `{"name": "Операції комірника", "model_id": "stock.picking", "domain": "[('state','in',['assigned','confirmed'])]", "context": "{}", "sort": "[]", "action_id": "$дія_місця"}`
325. **перевірити** `ir.ui.menu` домен `[["name", "=", "Операції комірника"]]`
   - очікувати кількість: 1
326. **створити** `ir.ui.view` (тільки якщо немає `[["name", "=", "hr.employee.місце.Робоче місце кадровика"]]`) → назвати `$подання_місця`
   - значення: `{"name": "hr.employee.місце.Робоче місце кадровика", "model": "hr.employee", "type": "list", "arch": "<list string=\"$елемент.назва\"><field name=\"display_name\"/></list>"}`
327. **створити** `ir.actions.act_window` (тільки якщо немає `[["name", "=", "Робоче місце кадровика"], ["res_model", "=", "hr.employee"]]`) → назвати `$дія_місця`
   - значення: `{"name": "Робоче місце кадровика", "res_model": "hr.employee", "view_mode": "list,form", "view_id": "$подання_місця", "domain": "[('active','=',True)]", "context": "{}"}`
328. **знайти_xmlid** → назвати `$меню_батько`
   - зовнішній id: `hr.menu_hr_root`
329. **знайти_xmlid** → назвати `$роль_місця`
   - зовнішній id: `hr.group_hr_user`
330. **створити** `ir.ui.menu` (тільки якщо немає `[["name", "=", "Робоче місце кадровика"], ["parent_id", "=", "$меню_батько"]]`)
   - значення: `{"name": "Робоче місце кадровика", "parent_id": "$меню_батько", "action": "$дія_місця", "group_ids": [[4, "$роль_місця"]], "sequence": 90}`
331. **створити** `ir.filters` (тільки якщо немає `[["name", "=", "Робоче місце кадровика"], ["model_id", "=", "hr.employee"]]`)
   - значення: `{"name": "Робоче місце кадровика", "model_id": "hr.employee", "domain": "[('active','=',True)]", "context": "{}", "sort": "[]", "action_id": "$дія_місця"}`
332. **перевірити** `ir.ui.menu` домен `[["name", "=", "Робоче місце кадровика"]]`
   - очікувати кількість: 1

## Поза базою — робить людина

- База р.1 · оглядова сесія 1 година із записом
- Міграція довідників р.3 · перенесення відкритих замовлень і боргів
- Міграція довідників р.3 · перенесення відкритих замовлень і боргів
- Навчання команди р.1 · демонстрація наскрізного процесу
- CRM р.1 · картка клієнта з контактами
- Продажі р.2 · погодження КП у браузері за посиланням: клієнт приймає й підписує без реєстрації (штатний підпис у замовленні)
- Підписки р.3 · розширення (upsell) з пропорційним донарахуванням; зменшення — без повернення коштів
- Оренда р.1 · видача й повернення
- Оренда р.1 · договір і рахунок

## Рецепта ще немає — збирати руками за журналом

- Склад р.2
- Міграція довідників р.1
- CRM р.2
- Інтернет-магазин р.1
- Інтернет-магазин р.2
- Точка продажу р.1
- Точка продажу р.2
- Закупівлі р.1
- Виїзні роботи р.1
- Планування р.1
- Планування р.2
- Сервісні заявки р.1
- Сервісні заявки р.2
- Обслуговування обладнання р.1
- Витрати співробітників р.1
- Персонал і відпустки р.1
- Персонал і відпустки р.2
- Документи й підписи р.1
- Дашборди й звіти р.1
- Дашборди й звіти р.2

